import SideNav from "./SideNav";

const Settings = () => {
  return (
    <section className="flex">
      <SideNav />
     
      <div>
        <h1>setting page</h1>
      </div>
    </section>
  );
};

export default Settings;
